name: Auto_deployment_front_angular

on:
  push:
    branches:
      - dev

jobs:
  install:
    runs-on: self-hosted

    steps:

    - name: Installer les dépendances
      run: |
        ls
        npm install
